class Thread {
  int id;
  String subject;
  String author;
  int numArticles;
  String postDate;
  String lastPostDate;

  Thread({this.id, this.subject, this.author, this.numArticles, this.postDate, this.lastPostDate});
}
